{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}} - Modification d'une cible
{% endblock %}

{% block body %}
	<h2>Modifier la cible {{editParameterTarget.parameterTargetTarget}}</h2>

{# {'method' : 'POST'}, #}
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}