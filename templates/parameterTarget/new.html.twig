{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}} - Ajout d'une cible
{% endblock %}

{% block body %}
		<h2>Ajout d'une cible</h2>
		{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
		<a href="list">Retour à la liste</a>
{% endblock %}