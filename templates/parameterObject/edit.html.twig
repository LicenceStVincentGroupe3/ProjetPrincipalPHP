{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}} - Modification d'un objet
{% endblock %}

{% block body %}
	<h2>Modifier l'objet {{editParameterObject.parameterObjectObject}}</h2>

{# {'method' : 'POST'}, #}
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}